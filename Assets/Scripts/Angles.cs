using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace OgreMaths
{
    class Angles
    {
        public const float DEG_270 = (float)Math.PI * 3 / 2;
        public const float DEG_180 = (float)Math.PI;
        public const float DEG_90  = (float)Math.PI / 2;
        public const float DEG_45  = (float)Math.PI / 4;
        public const float DEG_360 = (float)Math.PI * 2;
        public const float DEG_1   = (float)Math.PI * 2 / 360;
    }
}
